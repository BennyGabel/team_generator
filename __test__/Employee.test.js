const Employee = require('../lib/Employee')

test("Return Name", () => {
    const EmployeeInfo = new Employee("First", "Id", "email@jeje.com")

    expect(EmployeeInfo.name).toBe("First")
})

test("Return Name", () => {
    const EmployeeInfo = new Employee("First", "Id", "email@jeje.com")

    expect(EmployeeInfo.name).toBe("First")
})

test("Return Name", () => {
    const EmployeeInfo = new Employee("First", "Id", "email@jeje.com")

    expect(EmployeeInfo.name).toBe("First")
})
